# Notices

UsageMax first-party source in this repository is released under the MIT
License in [`LICENSE`](LICENSE). The publishable CLI package also declares MIT
in `packages/cli/package.json` and retains its package license file.

The CLI bundles `ccusage` and its transitive dependencies. Those dependencies
remain under their own licenses; their license and attribution files are
retained in the installed package dependency tree and are not relicensed by
UsageMax. Review the exact dependency lockfile and package metadata for the
version being built before distribution.

The web deployment uses third-party hosted services, including Vercel, Convex,
and WorkOS. Their service terms and trademarks are not part of this source
license.
